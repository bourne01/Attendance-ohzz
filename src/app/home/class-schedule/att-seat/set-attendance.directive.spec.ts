import { SetAttendanceDirective } from './set-attendance.directive';

describe('SetAttendanceDirective', () => {
  it('should create an instance', () => {
    const directive = new SetAttendanceDirective();
    expect(directive).toBeTruthy();
  });
});
