import { ClassBackgroundDirective } from './class-background.directive';

describe('ClassBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
